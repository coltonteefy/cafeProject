export class User {
  // first_name: string;
  // last_name: string;
  // email: string;
  // username: string;
  // password: string;
  // role: string;

  constructor(public first_name = '',
              public last_name = '',
              public email = '',
              public username = '',
              public password = '') {
  }
}
